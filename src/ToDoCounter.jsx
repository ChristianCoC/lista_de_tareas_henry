// eslint-disable-next-line react/prop-types
const TodoCounter = ({ totalTasks }) => {

  return (
    <div>
      <h2>Total de Tareas: {totalTasks}</h2>
    </div>
    
  )
};

export default TodoCounter;